# AGENTS.md

Generated by `bootstrap-agents`. Customize for this repository.

## Start Here

- Read `README.md`, `docs/` before coding.
- If `AGENTS.local.md` exists, read it too (personal, uncommitted defaults).

## Repo Context

- Detected stack(s): rust

## Common Commands (auto-detected; verify)

- `cargo test`
- `cargo fmt`
- `cargo clippy`

## Guardrails

- Ask 1–5 clarifying questions when requirements are underspecified.
- Prefer minimal, targeted diffs; follow existing patterns.
- Do not run destructive commands (e.g., `git reset --hard`, `git clean -fd`, `rm -rf`) unless explicitly requested.
- Do not commit or push unless explicitly requested.
- Treat production systems as read-only by default.
- Never deploy code, artifacts, or source hotfixes directly to production from Codex or any spawned subagent.
- No code may reach production except through a reviewed PR and the normal deployment path.
- If a production issue reveals a code bug, diagnose it, prepare the fix in a branch/PR, and stop there unless the user explicitly asks for a normal reviewed release action.
- If a Slack thread, Telegram chat, or other inbound channel includes MP4s, screen recordings, audio notes, or other media evidence, route analysis through the dedicated media-analysis skill instead of relying on guesswork from screenshots or thread text alone.
- Let long-running coding agents continue until they finish, the user explicitly cancels them, or they are clearly wedged/dead. Do not kill coding agents just because they have been running for a long time.
- Treat shared Slack channels, shared Slack threads, and Slack group DMs as product/engineering-only surfaces.
- Do not disclose private owner context in shared Slack: no family/household/health/travel details, private contact info, home address, birthdays, pets, or unrelated personal preferences.
- Anyone in the company Slack workspace may speak to Linus, but Slack remains an engineering/product surface by default.
- Only direct Slack DM with Sunil may use private owner context; all other Slack surfaces must stay product/engineering-only.
- In shared Slack, do not reveal internal machine names, repo paths, branch names, commit hashes, ports, tokens, local paths, or model/worker/tool brand names unless Sunil explicitly asks in a private/operator context.
- In shared Slack, always reply in the same originating channel/thread or the same group DM. Never move the conversation to `#eng-triage` or any other channel unless Sunil explicitly asks.
- In shared Slack, do not dump internal exploration or repeated dead ends. No `let me check`, `now let me try`, `I think it might be`, or similar running commentary.
- In shared Slack, do not present a root cause as fact until it is backed by direct evidence. If it is still a hypothesis, label it as a hypothesis.
- Default issue-investigation sequence unless Sunil explicitly says otherwise: reproduce locally, capture screenshot/evidence of the failure, isolate the cause, validate the fix locally, capture screenshot/evidence of the fixed state, then prepare or update the PR.
- If Sunil gives an explicit debugging or testing sequence in Slack or another operator channel, follow that exact order and treat it as the durable default for similar issue work unless he explicitly overrides it later.
- If a bug depends on customer- or project-specific data shape, prefer reproducing with the exact data shape locally via approved readonly production export/seed before inventing new harnesses or speculative synthetic setups.
- If exact local reproduction is blocked and the approved readonly data path is not available, ask once for the missing artifact that would collapse uncertainty fastest, usually a short screen recording, exact fixture, or missing access.
- Do not spend long public or private loops hand-constructing partial local data models when an exact readonly data clone or a customer recording would answer the question faster.
- In customer-facing bug work, prefer one verified workaround over a menu of speculative workarounds. If no workaround is verified, say so plainly instead of listing guesses.
- After two failed repro pivots without materially new evidence, stop changing tactics publicly and collapse to the best next path: exact readonly data clone, missing artifact request, or clearly scoped blocker.
- Before opening, updating, or asking for review on any engineering PR, require Jira linkage.
- PR workflow rule:
- first search the ENG Jira board for an existing relevant ticket
- if a relevant ticket exists, use it
- if no relevant ticket exists, create one on the ENG board before opening the PR
- PR titles must begin with the Jira key, for example `ENG-123 Fix multi-column answer editing`
- PR bodies should include the Jira key or Jira link so the code change and ticket stay tied together
- Do not open engineering PRs without a Jira issue unless Sunil explicitly overrides that rule.
