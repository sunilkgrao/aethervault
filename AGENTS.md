# AGENTS.md

Generated by `bootstrap-agents`. Customize for this repository.

## Start Here

- Read `README.md`, `docs/` before coding.
- If `AGENTS.local.md` exists, read it too (personal, uncommitted defaults).

## Repo Context

- Detected stack(s): rust

## Common Commands (auto-detected; verify)

- `cargo test`
- `cargo fmt`
- `cargo clippy`

## Guardrails

- Ask 1–5 clarifying questions when requirements are underspecified.
- Prefer minimal, targeted diffs; follow existing patterns.
- Do not run destructive commands (e.g., `git reset --hard`, `git clean -fd`, `rm -rf`) unless explicitly requested.
- Do not commit or push unless explicitly requested.
- Treat production systems as read-only by default.
- Never deploy code, artifacts, or source hotfixes directly to production from Codex or any spawned subagent.
- No code may reach production except through a reviewed PR and the normal deployment path.
- If a production issue reveals a code bug, diagnose it, prepare the fix in a branch/PR, and stop there unless the user explicitly asks for a normal reviewed release action.
- If a Slack thread, Telegram chat, or other inbound channel includes MP4s, screen recordings, audio notes, or other media evidence, route analysis through the dedicated media-analysis skill instead of relying on guesswork from screenshots or thread text alone.
- Let long-running coding agents continue until they finish, the user explicitly cancels them, or they are clearly wedged/dead. Do not kill coding agents just because they have been running for a long time.
- Treat shared Slack channels, shared Slack threads, and Slack group DMs as product/engineering-only surfaces.
- Do not disclose private owner context in shared Slack: no family/household/health/travel details, private contact info, home address, birthdays, pets, or unrelated personal preferences.
- Anyone in the company Slack workspace may speak to Linus, but Slack remains an engineering/product surface by default.
- Only direct Slack DM with Sunil may use private owner context; all other Slack surfaces must stay product/engineering-only.
- In shared Slack, do not reveal internal machine names, repo paths, branch names, commit hashes, ports, tokens, local paths, or model/worker/tool brand names unless Sunil explicitly asks in a private/operator context.
