--- a/dist/providers/anthropic.js
+++ b/dist/providers/anthropic.js
@@ -249,7 +249,8 @@ export const streamAnthropic = (model, context, options) => {
                     if (event.delta.stop_reason) {
                         output.stopReason = mapStopReason(event.delta.stop_reason);
                     }
-                    output.usage.input = event.usage.input_tokens || 0;
+                    // Only update input if provided (message_delta typically only has output_tokens)
+                    if (event.usage.input_tokens) output.usage.input = event.usage.input_tokens;
                     output.usage.output = event.usage.output_tokens || 0;
                     output.usage.cacheRead = event.usage.cache_read_input_tokens || 0;
                     output.usage.cacheWrite = event.usage.cache_creation_input_tokens || 0;
